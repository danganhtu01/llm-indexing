# Rust
target/

# Downloaded data (fetched by llm-index fetch-data; not committed)
data/dict/
data/tessdata/
data/vision/

# Model caches (fastembed / prefetch-models / fetch-data --vision; provisioned
# at docker build or by fetch-data, never committed)
models/

# Index outputs (generated; never commit indexed data)
index_out/
*.sqlite
*.sqlite-*
manifest.jsonl
catalog.csv
sidecar/
reports/
logs/

# Secrets / local
.env
*.local

# OS
Thumbs.db
desktop.ini
.DS_Store

# Agent worktrees / scratch (never commit; symlinks are machine-specific)
.claude/
# `target/` above does not match a SYMLINK named `target` (see the 2026-07-26
# snapshot commit that force-added one pointing at the old Hetzner volume).
target
