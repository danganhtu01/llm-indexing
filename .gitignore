# Rust
target/

# Downloaded data (fetched by llm-index fetch-data; not committed)
data/dict/
data/tessdata/
data/vision/

# Model caches (fastembed / prefetch-models / fetch-data --vision; provisioned
# at docker build or by fetch-data, never committed)
models/

# Index outputs (generated; never commit indexed data)
index_out/
*.sqlite
*.sqlite-*
manifest.jsonl
catalog.csv
sidecar/
reports/
logs/

# Secrets / local
.env
*.local

# OS
Thumbs.db
desktop.ini
.DS_Store
